# shared-contracts

This directory hosts frozen v1 shared contracts for frontend-backend integration.

Rules:

1. `v1` contracts are backward-compatible only.
2. Breaking changes require a new versioned manifest.
3. Contract updates must update `v1-freeze-manifest.json` and checklist docs.

